const image = document.getElementById('intro-img');
const iconbuttons = document.getElementById('intro-icons');

window.addEventListener('scroll', function() {
    iconbuttons.style.marginBottom = '3em';
});